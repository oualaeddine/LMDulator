<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dz.da3sou9a.oualaeddine.lmdulator">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logof"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".activities.mainUi.mainTable.activity.MainActivity"
            android:label="@string/app_name">
            <!--   <intent-filter>
                   <action android:name="android.intent.action.MAIN" />

                   <category android:name="android.intent.category.LAUNCHER" />
               </intent-filter>-->
        </activity>

        <activity
            android:name=".activities.mainUi.Loader"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.mainUi.Customizer"
            android:label="@string/title_activity_customizer" />
        <activity
            android:name=".activities.mainUi.FirstTimeSetup"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_first_time_setup" />
        <activity
            android:name=".activities.mainUi.Launcher"
            android:label="@string/title_activity_launcher" />
        <activity
            android:name=".activities.login.LoginMain"
            android:label="@string/app_name" />
        <activity android:name=".activities.login.LoginActivity" />
        <activity android:name=".activities.login.SignupActivity" />
        <activity
            android:name=".activities.mainUi.Settings"
            android:label="@string/title_activity_user_settings" />
        <activity android:name=".activities.mainUi.UserSettings" />
        <activity android:name=".activities.mainUi.dashboard.Dashboard" />

        <!--
     ATTENTION: This was auto-generated to add Google Play services to your project for



     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />


    </application>

</manifest>